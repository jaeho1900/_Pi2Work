# #                    주석
# *                    /를 제외한 모든 문자열
# /                    /부터 시작하는 경로패턴은 하위폴더를 포함하지 않음
#                      /를 마지막에 적는 경로패턴은 폴더에 있는 모든 파일을 무시
# !                    !로 시작하는 패턴은 무시하지 않음
# **                   특정폴더 하위에 위치한 모든 디렉토리
# {,}                  {}안에 ,로 구분된 각각의 문자열과 매칭
# [-]                  []안에서 -사이의 모든문자에 매칭

# *.js                 확장자가 .js 인 모든 파일을 무시
# /*.a                 현재 폴더의 확장자가 .a 인 모든파일을 무시
# folder_name/         해당 폴더의 모든 파일을 무시
# !test.js             test.js 파일은 무시하지 않음
# src/**/*.txt         src 하위의 모든폴더에서 txt확장자 파일 무시
# /*.{js,ts}           현재폴더의 모든 js, ts 확장자 파일 무시 (하위폴더에는 미적용)
# /ex[1-3].js          현재폴더의 ex1.js ex2.js ex3.js 파일 무시

yemoonsa/